import { View, Text } from 'react-native';

function HistoryScreen({ navigation }) {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>History Screen in dir</Text>
    </View>
  );
}

export default HistoryScreen;
